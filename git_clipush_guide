
How to get the next page: In Create a new repository page, only input the Repository name(clitesta), then click Create repository

Quick setup — if you’ve done this kind of thing before
or HTTPS | SSH   https://github.com/RobertCheng1/clitesta.git


We recommend every repository include a README, LICENSE, and .gitignore.
…or create a new repository on the command line

echo "# clitesta" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/RobertCheng1/clitesta.git
git push -u origin master

…or push an existing repository from the command line

git remote add origin https://github.com/RobertCheng1/clitesta.git
git push -u origin master

…or import code from another repository

You can initialize this repository with code from a Subversion, Mercurial, or TFS project.



----
Then follow the above guide"create a new repository on the command line" in your loacl shell. 
You can push everything you want to push


